import {
  scene,
  camera,
  renderer,
  sims,
  propRegistry,
  tileRegistry
} from "./main.js";

import {
  initEditor
} from "./editor.js";

initEditor({

  scene,

  camera,

  renderer,

  sims,

  propRegistry,

  tileRegistry
});